import { OidcCookieStorage } from './oidc-cookie-storage';

describe('OidcCookieStorage', () => {
  it('should create an instance', () => {
    expect(new OidcCookieStorage()).toBeTruthy();
  });
});
